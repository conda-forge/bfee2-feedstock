{% set name = "BFEE2" %}
{% set version = "3.1.1.post1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: 880a6e5a0d94751e03fadffa4e0bf47543515081aebb4f6b092327a618b0f712

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:

  host:
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - appdirs
    - MDAnalysis
    - matplotlib-base
    - numpy <2.3
    - scipy
    - Pyside6
    - parmed
    - requests

test:
  requires:
    - python {{ python_min }}
  imports:
    - BFEE2

about:
  home: https://github.com/fhh2626/BFEE2
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE
  summary: Binding Free Energy Estimator 2
  description: |
    Binding free energy estimator (BFEE) is a python-based 
    software that automates absolute binding free energy
    calculations through either the alchemical or geometric
    route by molecular dynamics simulations.
  doc_url: https://github.com/fhh2626/BFEE2
  dev_url: https://github.com/fhh2626/BFEE2/tree/dev

extra:
  recipe-maintainers:
    - HanatoK
    - fhh2626
